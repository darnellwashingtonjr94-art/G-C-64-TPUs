apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: g-c-64-tpus-route
  namespace: default
spec:
  hosts:
    - "g-c-64-tpus.local"
  http:
    - route:
        - destination:
            host: g-c-64-tpus
            port:
              number: 80
          weight: 100
